# Copy the required values from your Vercel project settings.
# Never commit real credentials to this file.

POSTGRES_URL=

# Generate locally with: openssl rand -base64 32
AUTH_SECRET=
AUTH_URL=http://localhost:3000/api/auth
